apiVersion: cloudstackctl/v1
kind: Application
metadata:
  name: example-app
spec:
  components:
    - name: frontend
      virtualMachineSpec: basic-vm-spec
      replicas: 2
      healthChecks:
        - type: ping
          interval: 10s
          timeout: 5s
